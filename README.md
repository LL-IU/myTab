# MyTab

我的标签页

## 介绍

自制的浏览器起始页，自己用

**已上架 Microsoft Edge 扩展商店**（为避免重名冲突，商店名改为 **OwnTab**）：

<https://microsoftedge.microsoft.com/addons/detail/nhadmjoefpjpafabbghgfmlekfmbapce>

版本迭代简表：
+ **v1.0**：一个时间和一个搜索框
+ **v2.0**：增加快捷链接（A–Z 键盘布局）、自定义背景与图标、百度联想词、颜色切换等
+ **v3.0**：搜索引擎切换（含自定义引擎）、文字图标与自动获取 favicon、统一事件绑定修复冲突
+ **v3.1**：现代化重构——名称栏与悬浮提示、编辑菜单三按钮、默认白底、菜单互斥、自定义引擎弹窗等

### 搜索框效果

1. 进入主页时自动聚焦
2. 聚焦时放大，出现图标，背景图放大，颜色、透明度等变化
3. 点击区域外取消聚焦，并且缩短

#### v2.0

1. 在初版基础上，增加了网站快捷链接
2. 链接可以通过右键键盘格添加到对应位置
3. 可以自定义图标，~~暂只支持链接~~，已支持选择本地图片，选好一个要刷新一次页面。图标同时设置了链接和本地文件时，优先显示本地
4. 不会后端，不能保存自定义修改
5. 用localStorage保存自定义的网址
6. 可以按键操作：
   + esc可以聚焦或取消聚焦搜索框，聚焦搜索框时enter搜索
   + 右键出现键盘格菜单后，esc菜单消失，上、下调整光标所在输入框，enter添加
   + 在右键菜单消失并且搜索框不聚焦时，按对应键盘的字母按键可以进入对应已设置的网址
7. 可以通过在右键菜单的“网址”栏输入“clear”清除对应的localStorage记录
8. 加入了联想词，可以点击前往搜索，联想词带空格不能正常搜索，只能搜索到空格前的部分
9. 右键背景可以更改背景，~~图片大小限制在2M内~~，增加选项以清除添加的背景，通过压缩图片让其能被localstorage存储，不限制一般大小的图片
10. 右键背景可以设置快捷链接的显示和隐藏
11. 添加快捷链接的hover效果，视觉体验改善

12. 添加修改字体颜色的按钮，自定义修改时间和快捷链接字母提示的字体颜色，目前支持黑白切换。
13. 克隆到本地，提交测试。2024.8.2

#### v3.0

当前分支 v3.0-dsh-20260818：基于 v2.0 的代码优化版（DeepSeek Harness 协助，2026.8）。localStorage 键位保持不变，原有自定义数据完全兼容：

1. 统一事件绑定：改用 addEventListener，修复 window.onkeydown / window.onclick 被反复覆盖导致的相互冲突
2. 修复联想词：正确域名 + URL 编码（带空格可正常搜索）+ textContent 防注入 + 输入防抖
3. 修复清除逻辑：在网址栏输入 clear 才清除记录（原代码只判断 c），并同步重置链接与图标 DOM
4. 修复图标替换：添加/更换本地图标无需刷新页面
5. 修复搜索时序：点击搜索按钮不再依赖失焦取值，搜索后保留已输入内容
6. 交互优化：空链接点击直接弹出编辑菜单；输入网址自动补全 https://
7. 代码结构：移除 HTML 内联事件，逻辑收进 IIFE，消除全局变量
8. 性能与适配：字体 font-display: swap 不阻塞首屏；搜索框/联想词响应式宽度；≤900px 小屏适配
9. 快捷键扩展：Alt+字母 直接唤出对应格子的编辑菜单
10. 搜索引擎切换：点击搜索框左侧按钮切换百度/必应/谷歌/搜狗，可自定义搜索引擎（搜索链接含 {q} 占位符），选择持久化；联想词仅百度可用
11. 文字图标：有链接无图标时自动尝试获取网站 favicon，失败则用域名首字母生成彩色文字图标；图标栏输入普通文字（非图片链接）时以文字形式展示

#### v3.1（现代化重构，2026.8）

在 v3.0 基础上完成代码现代化，并补全了「后续想法」中剩余的全部功能：

1. 代码现代化：ES6+（const/let、箭头函数、模板字符串）、event.key、语义化类名、CSS 变量、:focus-visible、prefers-reduced-motion
2. 新增「名称」栏与悬浮提示：编辑菜单可填网站名称；鼠标悬停快捷链接时浮现跟随鼠标的提示，显示名称与网址
3. 编辑菜单重构：底部新增「清除网址 / 清除图标 / 本地图片」三个按钮；右键已配置的链接时自动预填网址、名称、图标（在线链接 / 文字 / 本地图片文件名）
4. 移除 Alt+字母唤出菜单（避免与其他快捷键冲突）
5. 默认无背景图片，使用白色作为底色；清除背景回到白色；时间 / 链接文字默认黑色（适配白底）
6. 右键空白处菜单新增「使用方法」，点击展开使用说明
7. 网站图标获取失败时显示汉字「无」
8. 右键快捷链接菜单与空白处菜单 UI 优化：毛玻璃、圆角、阴影、hover 反馈；打开任一菜单时自动关闭其他浮层，避免多个菜单同时显示冲突
9. 自定义搜索引擎改为弹窗表单（替换 prompt）
10. localStorage 键位保持不变，原有自定义数据完全兼容；新增名称（81-106）与本地图标文件名（108-133）键位

#### v3.2（测试问题修复，2026.8）

针对「自己测试」记录的问题逐项修复：

1. 「使用方法」改为在右键菜单右侧展开，限定宽度与最大高度，内容可上下滚动；右侧空间不足时自动向左展开
2. 快捷链接格子的键盘字母提示固定在左上角，不再居中遮挡图标
3. 点击搜索引擎按钮切换引擎时，搜索框保持聚焦放大状态，不再缩小
4. 联想词方框样式修正：上下留白对称；宽度自动匹配搜索框宽度
5. 移除默认背景图 bg.jpg（默认白色底色，无背景图）

#### v3.3（模块化重构，2026.8）

代码结构重构，行为与 v3.2 完全一致，零依赖、无需构建、双击 index.html 仍可直接使用：

1. 单文件 mytab.js 拆分为 `js/` 目录下 7 个模块（经典脚本 + `window.MyTab` 命名空间协作；因浏览器拦截 file:// 下的 type="module"，故未用 ES Module，日后部署到 HTTP 服务器可无缝升级）：
   + `js/mytab-core.js`：DOM 引用与共享常量 / 工具
   + `js/mytab-storage.js`：localStorage 历史键位集中管理（旧数据完全兼容）
   + `js/mytab-time.js`：时间渲染
   + `js/mytab-search.js`：搜索 / 引擎切换与自定义 / 聚焦失焦样式 / 百度联想词
   + `js/mytab-links.js`：快捷链接 / 图标三态 / 右键编辑菜单 / 悬浮提示
   + `js/mytab-bg.js`：背景 / 显隐链接 / 颜色切换 / 使用方法面板
   + `js/mytab-init.js`：全局键盘 / 点击 / 右键事件与浮层互斥 + 启动编排（最后加载）
2. 加载顺序固定：core → storage → time → search → links → bg → init（即 index.html 中脚本顺序）
3. 冒烟测试 `tools/smoke-test.js` 同步改为按顺序加载 7 个模块，56 项断言全部通过

#### v3.4（Edge / Chrome 新标签页扩展，2026.8）

在 v3.3 基础上增加浏览器扩展支持（Manifest V3，Chromium 内核通用，Edge / Chrome 均可）：

1. 新增 `manifest.json`：`chrome_url_overrides.newtab` 覆盖新标签页；`host_permissions` 授予百度联想词域名
2. 新增 `icons/` 目录：由 favicon.ico 生成的 16 / 48 / 128 PNG 图标
3. 联想词双路径：扩展环境（MV3 CSP 拦截远程脚本）改用 `fetch` + host_permissions 拉取并剥壳解析 JSONP 数据；网页版保持原 JSONP 方案，行为一致
4. 网页版完全不受影响：同一套代码双端可用，`js/` 模块零结构性改动（仅 search.js 内部增加环境分支）

#### v3.4.1（数据备份，2026.8）

右键空白处菜单新增「导出数据 / 导入数据」（网页版与扩展版通用）：

1. 导出：生成 `mytab-backup-时间戳.json`，包含全部 26 个链接槽（网址 / 图标 / 本地图标 / 名称）与全部设置项（背景 / 显隐 / 颜色 / 自定义引擎 / 引擎索引）
2. 导入：先校验文件结构（app 标记、26 槽位、设置对象），校验通过后全量恢复并即时刷新界面（无需刷新页面）；不合法文件直接拒绝，不影响现有数据
3. 新增 `js/mytab-data.js` 数据模块（storage → data → … → init 加载顺序）；存储层新增 `clearAll()`；links / bg 的恢复逻辑抽为可重入的 `reload()` 供导入后调用
4. 使用场景：备份自定义数据、在浏览器或设备间迁移（例如从网页版导入到扩展版）
5. 冒烟测试新增导出 / 导入 10 项用例，共 68 项全部通过

#### v3.5（WebDAV 手动同步，2026.8）

右键空白处菜单新增「WebDAV 设置 / 立即同步」，可把全部自定义数据备份到自己的 WebDAV 网盘（坚果云、InfiniCloud 等均可）；**除手动点击「立即同步」外不做任何自动同步**：

1. 「WebDAV 设置」弹窗：功能开关 + 服务器地址 / 文件名 / 账号 / 密码（Basic 认证，经 TextEncoder 支持中文等非 Latin1 字符）。地址配置为两段：**服务器地址只填 WebDAV 根目录**（如 `https://dav.jianguoyun.com/dav/`），**文件名填相对根目录的完整路径**（如 `/mytab/mytab.json`）；**首次保存配置时自动在云端创建对应文件夹**（已存在则服务器返回 405 跳过），失败会弹窗提示；「测试连接」结果直接显示在弹窗内（401 / 404 / 网络错误分别提示）；「从云端恢复」拉取云端备份，校验通过后全量应用并即时刷新界面（WebDAV 配置本身不会被清除）
2. 「立即同步」：上传与「导出数据」同一格式的 JSON（26 个链接槽 + 全部设置项）到「根目录 + 文件名路径」拼接出的目标地址；服务器返回 409（父目录不存在）时自动 MKCOL 建目录并重试一次；未配置 / 未启用时自动打开设置弹窗引导；成功后记录上次同步时间
3. 权限（扩展版）：manifest 新增 `optional_host_permissions`，保存配置时动态申请对应站点的主机权限（用户手势内调用），拒绝授权则提示；未授权时「立即同步」明确拦截。网页版直接 fetch，能否连接取决于目标服务器的 CORS 策略（自建服务器开启 CORS 即可，坚果云等按扩展方式使用）
4. 配置存储：localStorage 键位 -7（WebDAV 配置）/ -8（上次同步时间）；配置不属于备份数据，「导入数据」/ clearAll 均不清除
5. 新增 `js/mytab-webdav.js` 模块（加载顺序 bg → webdav → init）；`js/mytab-data.js` 补充导出 `buildExport / validate / applyImport` 供云端恢复复用
6. 调试工具 `tools/test-webdav-server.js`：本地 WebDAV 模拟服务器（Basic 认证、409 建目录流程、CORS），配合任意静态 HTTP 服务可完整联调「配置 → 同步 → 恢复」全流程
7. 冒烟测试新增 WebDAV 用例 21 项（弹窗交互、配置持久化、首次保存自动建文件夹、PUT 认证与目标地址、409 重试、401 提示、测试连接、云端恢复、扩展权限申请与拦截等），共 89 项全部通过

侧载安装：
1. Edge 打开 `edge://extensions` → 开启「开发人员模式」
2. 「加载解压缩的扩展」→ 选择本目录（my-tab）
3. 新开标签页即为 MyTab；如联想词异常，检查扩展详情中百度域名的权限

#### 后续想法

+ ✅ 用快捷键alt+字母，唤出对应右键菜单（v3.0 已实现，v3.1 按想法移除，避免冲突）
+ ✅ 按搜索框左边的按钮可以更改搜索引擎，可自定义搜索引擎（v3.0 已实现）
+ ✅ 添加修改字体颜色的按钮，自定义修改时间和快捷链接字母提示的字体颜色（v2.0 已实现）
+ ✅ 增加用文字代替图标图片的功能 + 自动从网页获取图标（v3.0 已实现）
+ ✅ 右键空白处菜单里增加一个使用方法，点击展开文字框，里面写一些使用说明（v3.1 已实现）
+ ✅ 去掉alt+字母唤出对应右键菜单，会和别的快捷键冲突（v3.1 已移除）
+ ✅ 优化快捷链接添加网址的浮窗，增加一行用来写网页名字，鼠标停在快捷链接上时，浮现跟随鼠标的文字，显示网址和名字；把浮窗右侧选择本地图片做图标的按钮去掉，改为在浮窗底下新加一行，放上3个按钮，分别为清除网址、清除图标、选择本地图片做图标；右键已经填入网址的快捷链接时，在输入框显示已填入的网址，在名称栏显示填入的名称，在图标栏显示在线图标的链接或文字图标或本地图标的文件名（v3.1 已实现）
+ ✅ 默认无背景图片，使用白色作为底色（v3.1 已实现）
+ ✅ 优化右键快捷链接菜单和空白处菜单的UI显示效果（v3.1 已实现）
+ ✅ 检查获取网页的在线图标的功能，获取不到时显示汉字“无”（v3.1 已实现）

#### 自己测试
+ ✅ 右键空白处菜单，点击使用方法时，下方超出屏幕范围，改为点击使用方法后，在右侧展开内容，且限定展示高度和宽度，文字较多时，使文字内容能上下滚动（v3.2 已修复）
+ ✅ 快捷链接的格子内，键盘字母提示改为左上角，居中会影响图标显示（v3.2 已修复）
+ ✅ 点击搜索引擎图标进行更换搜索引擎时，使搜索框不进行因为失去焦点而进行的缩小变化（v3.2 已修复）
+ ✅ 输入搜索内容，出现联想词时，展示联想词的方框样式修改，底部距离文字距离和顶部距离文字距离不一致，方框宽度匹配搜索框宽度（v3.2 已修复）

